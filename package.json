{
  "version": "v0.0.1",
  "name": "devtest",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ devtest",
    "create-local": "graph create --node http://localhost:8020/ devtest",
    "remove-local": "graph remove --node http://localhost:8020/ devtest",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 devtest",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.42.2",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
